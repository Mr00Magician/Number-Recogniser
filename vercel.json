{
    "version": 2,
    "builds": [
        {
            "src": "*.py",
            "use": "@vercel/python"
        }
    ],
    "buildCommand": "flask --app server/app.py run",
    "routes": [
        {
            "src": "(.*)",
            "dest": "./server/app.py"
        }
    ]
}