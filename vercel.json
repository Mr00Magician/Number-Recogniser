{
    "version": 2,
    "builds": [
        {
            "src": "./app.py",
            "use": "@vercel/python"
        }
    ],
    "buildCommand": "pip install -r requirements.txt && python app.py",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "app.py"
        }
    ]
}