{
    "version": 2,
    "builds": [
        {
            "src": "server/app.py",
            "use": "@vercel/python"
        }
    ],
    "buildCommand": "pip install -r requirements.txt && python ./server/app.py",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/app.py"
        }
    ]
}